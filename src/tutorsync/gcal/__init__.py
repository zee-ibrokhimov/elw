"""Всё, что касается Google Calendar.

Пакет назван ``gcal``, а не ``google``: внутри пакета ``tutorsync.google`` любой
``import google.oauth2`` пришлось бы читать через призму «а не свой ли это
подпакет» — абсолютный импорт разрешил бы его верно, но человек, читающий код,
каждый раз спотыкался бы. Имя, не совпадающее с чужим неймспейсом, дешевле.
"""
