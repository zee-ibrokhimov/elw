# Разработка идёт на Windows, сборка и запуск — в Linux-контейнере.
# Без этого в репозиторий уезжают CRLF, и всё, что читается построчно
# (yaml, ini, шаблоны миграций), начинает вести себя по-разному.
* text=auto eol=lf
*.png binary
*.eml -text
